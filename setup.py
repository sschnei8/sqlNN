from setuptools import setup

setup(
    name="sqlNN",
    install_requires=[
        "duckdb",
        "pandas",
        "matplotlib",
        "numpy",
        "python-dotenv",
    ]
)















